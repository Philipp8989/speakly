---
phase: 05-ai-commands
plan: 03
type: execute
wave: 3
depends_on:
  - 05-01
  - 05-02
files_modified: []
autonomous: false
requirements:
  - AI-01
  - AI-02
  - AI-03

must_haves:
  truths:
    - "User can record speech, see transcript, click a command, and get reformulated text pasted"
    - "Raw 'Einfuegen' button pastes transcript directly without Claude API"
    - "Auto-dismiss at 10s pastes raw transcript"
    - "Disabled buttons shown when no Anthropic API key is set"
    - "API error shows fallback message and pastes raw transcript"
  artifacts:
    - path: "src-tauri/src/ai/mod.rs"
      provides: "Claude API integration"
      contains: "call_claude_api"
    - path: "src/components/TrayPopup.tsx"
      provides: "7-state UI"
      contains: "command-select"
    - path: "src/App.tsx"
      provides: "Full AI command flow"
      contains: "apply_ai_command"
  key_links:
    - from: "src/App.tsx"
      to: "apply_ai_command"
      via: "invoke"
      pattern: "invoke.*apply_ai_command"
---

<objective>
Human verification of all 4 Phase 5 success criteria: AI command flow end-to-end, raw insert, auto-dismiss, disabled state.

Purpose: Confirms Phase 5 is shippable before moving to Phase 6.
Output: Verified phase with SUMMARY documenting any issues found.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/05-ai-commands/05-CONTEXT.md
@.planning/phases/05-ai-commands/05-01-SUMMARY.md
@.planning/phases/05-ai-commands/05-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build and run the app for verification</name>
  <files></files>
  <action>
Run the dev server to prepare for human testing:

```bash
cd /Users/philipppotzinger/Desktop/speakly
cargo tauri dev
```

If there are compile errors, fix them before proceeding to the checkpoint.
Confirm the app launches and the tray icon appears.
  </action>
  <verify>
    <automated>cd /Users/philipppotzinger/Desktop/speakly/src-tauri && cargo build 2>&1 | tail -5</automated>
  </verify>
  <done>App launches without errors, tray icon visible</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Full AI Commands feature:
    - Rust ai/mod.rs with 6 preset commands calling Claude API (claude-3-5-haiku-latest)
    - TrayPopup extended to 7-state machine (+ command-select, reformulating, reformulated)
    - App.tsx wired with full command flow, auto-dismiss (10s), cancel via hotkey, error fallback
    - inject_raw_text command for direct paste without Claude
  </what-built>
  <how-to-verify>
    **SC-1: AI command flow (requires valid Anthropic API key in settings)**
    1. Open Speakly settings → confirm Anthropic API key is entered
    2. Press hotkey → speak a sentence in German → release hotkey
    3. Popup should show "Befehl waehlen" state with transcript text and 6 pill buttons
    4. Click "Formeller" button
    5. Popup shows "Formuliere um..." with spinner
    6. After response: reformulated text appears in green, then gets pasted into focused text field
    7. Popup returns to idle

    **SC-2: Raw insert (Einfuegen button)**
    1. Press hotkey → speak → popup shows command buttons
    2. Click "Einfuegen" button (green pill)
    3. Raw transcript appears in text field immediately (no Claude API call)
    4. Popup shows "Eingefuegt" → idle

    **SC-3: Auto-dismiss (10 second timer)**
    1. Press hotkey → speak → popup shows command buttons
    2. Do NOT click anything
    3. After 10 seconds, raw transcript should appear in focused text field automatically
    4. Popup returns to idle

    **SC-4: Disabled state (no API key)**
    1. Open settings → clear Anthropic API key → save
    2. Press hotkey → speak → popup shows command buttons
    3. All 6 AI command pill buttons should be greyed out / disabled
    4. Hovering a disabled button shows tooltip "Anthropic API-Key in Einstellungen eingeben"
    5. "Einfuegen" button should still be active and work

    **SC-4b: Cancel via hotkey**
    1. Press hotkey → speak → popup shows command buttons
    2. Press hotkey again (do not speak)
    3. Popup should return to idle WITHOUT pasting anything
  </how-to-verify>
  <resume-signal>Type "approved" if all criteria pass, or describe which criteria failed and what was observed</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Human tester → app | Manual verification only; no new attack surface |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-08 | Information Disclosure | Dev build logging | accept | Dev-mode console.log present — acceptable for development; prod build removes debug output |
</threat_model>

<verification>
All 4 Phase 5 success criteria verified by human:
1. AI command flow: transcript → command → Claude API → paste
2. Raw insert: "Einfuegen" button pastes without Claude
3. Auto-dismiss: 10s inactivity → raw paste
4. Disabled buttons when no Anthropic API key
</verification>

<success_criteria>
Human confirms all 4 success criteria in the checkpoint pass.
Phase 5 marked complete in ROADMAP.md and STATE.md.
</success_criteria>

<output>
After completion, create `.planning/phases/05-ai-commands/05-03-SUMMARY.md`
Update ROADMAP.md: Phase 5 status → Complete, date → today.
Update STATE.md: current focus → Phase 6.
</output>

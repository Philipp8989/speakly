---
phase: 06-offline-fallback
plan: 02
type: execute
wave: 2
depends_on:
  - 06-01
files_modified:
  - src-tauri/src/stt/mod.rs
  - src-tauri/src/lib.rs
autonomous: true
requirements:
  - STT-04
  - STT-05
must_haves:
  truths:
    - "transcribe_and_inject liest stt_mode aus ConfigStore und leitet an cloud oder local"
    - "Auto-Modus: cloud-Versuch mit 3s Timeout; bei Netzwerkfehler automatisch local"
    - "Lokaler Modus: f32-Samples direkt an call_whisper_local() ohne WAV-Encoding"
    - "download_whisper_model und cancel_whisper_download sind als Tauri-Commands registriert"
    - "model_exists wird geprueft bevor lokale Transkription startet — fehlende Datei gibt verstaendliche Fehlermeldung"
  artifacts:
    - path: "src-tauri/src/stt/mod.rs"
      provides: "Re-exports fuer local und download Module"
      exports: ["call_whisper_api", "local", "download"]
    - path: "src-tauri/src/lib.rs"
      provides: "transcribe_and_inject mit stt_mode-Routing, neue Commands registriert"
      contains: "stt_mode"
  key_links:
    - from: "src-tauri/src/lib.rs transcribe_and_inject"
      to: "stt::local::call_whisper_local"
      via: "stt_mode == 'local' oder auto-fallback"
      pattern: "call_whisper_local"
    - from: "src-tauri/src/lib.rs transcribe_and_inject"
      to: "stt::call_whisper_api"
      via: "stt_mode == 'cloud' oder auto-primary"
      pattern: "call_whisper_api"
---

<objective>
Rust-Routing: stt/mod.rs bekommt Module-Deklarationen fuer local + download.
transcribe_and_inject in lib.rs erhaelt stt_mode-Router (cloud/local/auto).
Neue Tauri-Commands werden in invoke_handler registriert.

Purpose: Verbindet die in Plan 01 erstellten Funktionen mit dem bestehenden
transcribe_and_inject Command und macht den Offline-Fallback vollstaendig funktionsfaehig.

Output:
- stt/mod.rs um pub mod local + pub mod download erweitert
- transcribe_and_inject mit 3-Zweig-Router (cloud/local/auto)
- download_whisper_model + cancel_whisper_download in invoke_handler
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-offline-fallback/06-CONTEXT.md
@.planning/phases/06-offline-fallback/06-01-SUMMARY.md

<interfaces>
<!-- Aus Plan 01 neu erstellt -->
From src-tauri/src/stt/local.rs:
```rust
pub fn model_path(app: &tauri::AppHandle) -> Result<PathBuf, String>
pub fn call_whisper_local(app: &tauri::AppHandle, samples: &[f32], sample_rate: u32) -> Result<String, String>
```

From src-tauri/src/stt/download.rs:
```rust
pub const WHISPER_BASE_URL: &str = "...";
pub const WHISPER_BASE_SIZE: u64 = 147_964_211;
pub fn model_exists(app: &tauri::AppHandle) -> bool
#[tauri::command] pub async fn download_whisper_model(app: tauri::AppHandle) -> Result<(), String>
#[tauri::command] pub async fn cancel_whisper_download(app: tauri::AppHandle) -> Result<(), String>
```

<!-- Bestehende lib.rs Struktur — READ FIRST -->
From src-tauri/src/lib.rs (transcribe_and_inject):
```rust
async fn transcribe_and_inject(
    app: tauri::AppHandle,
    state: tauri::State<'_, RecordingState>,
) -> Result<String, String> {
    // WAV aus state.wav_buffer holen
    // call_whisper_api(&app, wav) aufrufen
    // inject_text(&app, &text) aufrufen
    // Events emittieren
}
```

From src-tauri/src/recording/mod.rs:
```rust
pub struct RecordingState {
    pub audio_buffer: Arc<Mutex<Vec<f32>>>,
    pub native_sample_rate: Arc<Mutex<u32>>,
    pub wav_buffer: Arc<Mutex<Option<Vec<u8>>>>,
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: stt/mod.rs — pub mod local und pub mod download deklarieren</name>
  <files>src-tauri/src/stt/mod.rs</files>
  <action>
Lies src-tauri/src/stt/mod.rs zuerst vollstaendig.

Ergaenze am Anfang der Datei (nach dem bestehenden Datei-Kommentar):
```rust
pub mod local;
pub mod download;
```

Die bestehende call_whisper_api() Funktion UNVERAENDERT lassen.
Nur die zwei mod-Deklarationen hinzufuegen.

Ergebnis: stt/mod.rs exportiert jetzt:
- call_whisper_api() (unveraendert)
- pub mod local (mit call_whisper_local, model_path)
- pub mod download (mit download_whisper_model, cancel_whisper_download, model_exists)

Validierung: `cargo check` muss fehlerlos durchlaufen.
</action>
  <verify>
    <automated>cd /Users/philipppotzinger/Desktop/speakly/src-tauri && cargo check 2>&1 | tail -20</automated>
  </verify>
  <done>
    - stt/mod.rs enthaelt `pub mod local;` und `pub mod download;`
    - cargo check laeuft fehlerlos
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: lib.rs — transcribe_and_inject mit stt_mode-Router + neue Commands registrieren</name>
  <files>src-tauri/src/lib.rs</files>
  <behavior>
    - Test 1: stt_mode="cloud" → call_whisper_api wird aufgerufen (WAV-Pfad)
    - Test 2: stt_mode="local" → call_whisper_local wird aufgerufen (f32-Samples)
    - Test 3: stt_mode="auto" + Netzwerkfehler innerhalb 3s → fallback auf local
    - Test 4: stt_mode="local" + Modell fehlt → Err("Whisper-Modell nicht gefunden...")
    - Test 5: stt_mode="local" + leere Samples → Err("Kein Audio...")
    Tests fuer Router-Logik ohne AppHandle: Hilfsfunktion route_stt_mode(mode: &str) -> SttMode enum testen
  </behavior>
  <action>
Lies src-tauri/src/lib.rs vollstaendig zuerst (READ FIRST).

1. Ergaenze am Anfang von lib.rs nach `mod ai;`:
```rust
// Phase 6: SttMode fuer Engine-Auswahl (D-01, D-02, D-05)
#[derive(Debug, PartialEq)]
enum SttMode { Cloud, Local, Auto }

fn parse_stt_mode(s: &str) -> SttMode {
    match s {
        "local" => SttMode::Local,
        "auto"  => SttMode::Auto,
        _       => SttMode::Cloud, // Default: cloud (D-02)
    }
}
```

2. transcribe_and_inject Command ERSETZEN (Phase 6 erweitert Phase 4):
```rust
#[tauri::command]
async fn transcribe_and_inject(
    app: tauri::AppHandle,
    state: tauri::State<'_, RecordingState>,
) -> Result<String, String> {
    use tauri::Emitter;
    use std::sync::atomic::Ordering;

    // stt_mode aus ConfigStore lesen (D-04: Schluessel seit Phase 1 vorhanden)
    let stt_mode = {
        let store = app.store("settings.json").map_err(|e| e.to_string())?;
        let mode_str = store.get("stt_mode")
            .and_then(|v| v.as_str().map(|s| s.to_string()))
            .unwrap_or_else(|| "cloud".to_string());
        parse_stt_mode(&mode_str)
    };

    // Verarbeitungs-State senden
    let _ = app.emit("transcription_state_changed", serde_json::json!({
        "state": "processing"
    }));

    // Engine-Router (D-03, D-04, D-05)
    let text = match stt_mode {
        SttMode::Cloud => {
            // Cloud: WAV-Puffer holen und Whisper API aufrufen
            let wav = {
                let lock = state.wav_buffer.lock().unwrap();
                lock.clone().ok_or_else(|| "Kein WAV-Puffer vorhanden".to_string())?
            };
            crate::stt::call_whisper_api(&app, wav).await?
        }

        SttMode::Local => {
            // Local: Modell pruefen, f32-Samples direkt weitergeben (D-09)
            if !crate::stt::download::model_exists(&app) {
                let err = "Whisper-Modell nicht gefunden — bitte in Einstellungen herunterladen";
                let _ = app.emit("transcription_state_changed", serde_json::json!({
                    "state": "error", "message": err
                }));
                return Err(err.into());
            }
            let (samples, rate) = {
                let buf = state.audio_buffer.lock().unwrap().clone();
                let rate = *state.native_sample_rate.lock().unwrap();
                (buf, rate)
            };
            // Synchron in eigenem Thread (D-08: blockiert nicht den async-Runtime)
            let app2 = app.clone();
            tokio::task::spawn_blocking(move || {
                crate::stt::local::call_whisper_local(&app2, &samples, rate)
            })
            .await
            .map_err(|e| format!("Thread-Fehler: {e}"))??
        }

        SttMode::Auto => {
            // Auto: Cloud-Versuch mit 3s Timeout; bei Netzwerkfehler → local (D-03)
            let wav = {
                let lock = state.wav_buffer.lock().unwrap();
                lock.clone().ok_or_else(|| "Kein WAV-Puffer vorhanden".to_string())?
            };

            let cloud_result = tokio::time::timeout(
                std::time::Duration::from_secs(3),
                crate::stt::call_whisper_api(&app, wav),
            )
            .await;

            match cloud_result {
                Ok(Ok(t)) => t,  // Cloud erfolgreich
                _ => {
                    // Netzwerkfehler oder Timeout → lokal fallback (D-03)
                    if !crate::stt::download::model_exists(&app) {
                        let err = "Auto-Modus: Offline erkannt, aber kein lokales Modell — bitte herunterladen";
                        let _ = app.emit("transcription_state_changed", serde_json::json!({
                            "state": "error", "message": err
                        }));
                        return Err(err.into());
                    }
                    let (samples, rate) = {
                        let buf = state.audio_buffer.lock().unwrap().clone();
                        let rate = *state.native_sample_rate.lock().unwrap();
                        (buf, rate)
                    };
                    let app2 = app.clone();
                    tokio::task::spawn_blocking(move || {
                        crate::stt::local::call_whisper_local(&app2, &samples, rate)
                    })
                    .await
                    .map_err(|e| format!("Thread-Fehler: {e}"))?? 
                }
            }
        }
    };

    // Leeres Ergebnis behandeln
    if text.trim().is_empty() {
        let _ = app.emit("transcription_state_changed", serde_json::json!({
            "state": "error", "message": "Keine Sprache erkannt"
        }));
        return Ok(String::new());
    }

    // Text injizieren
    if let Err(e) = crate::inject::inject_text(&app, &text).await {
        let _ = app.emit("transcription_state_changed", serde_json::json!({
            "state": "error", "message": e
        }));
        return Err(e);
    }

    let _ = app.emit("transcription_state_changed", serde_json::json!({
        "state": "done", "text": text.trim()
    }));

    Ok(text)
}
```

3. invoke_handler in run() erweitern: download_whisper_model und cancel_whisper_download hinzufuegen:
```rust
.invoke_handler(tauri::generate_handler![
    validate_openai_key,
    validate_anthropic_key,
    list_audio_input_devices,
    check_macos_permissions,
    toggle_recording,
    stop_recording_hold,
    transcribe_and_inject,
    apply_ai_command,
    inject_raw_text,
    crate::stt::download::download_whisper_model,   // Phase 6
    crate::stt::download::cancel_whisper_download,  // Phase 6
])
```

4. Unit-Tests ergaenzen (am Ende von lib.rs):
```rust
#[cfg(test)]
mod tests {
    use super::*;

    #[test]
    fn test_parse_stt_mode() {
        assert_eq!(parse_stt_mode("cloud"), SttMode::Cloud);
        assert_eq!(parse_stt_mode("local"), SttMode::Local);
        assert_eq!(parse_stt_mode("auto"),  SttMode::Auto);
        assert_eq!(parse_stt_mode(""),      SttMode::Cloud); // Default
        assert_eq!(parse_stt_mode("unbekannt"), SttMode::Cloud); // Default
    }
}
```

5. `cargo test -- tests::test_parse_stt_mode` ausfuehren → GREEN.
6. `cargo check` → fehlerlos.

WICHTIG: tokio::time::timeout benoetigt das "time" Feature in Cargo.toml:
tokio = { version = "1", features = ["rt-multi-thread", "time"] }
</action>
  <verify>
    <automated>cd /Users/philipppotzinger/Desktop/speakly/src-tauri && cargo test -- tests::test_parse_stt_mode 2>&1 && cargo check 2>&1 | tail -10</automated>
  </verify>
  <done>
    - transcribe_and_inject enthaelt stt_mode-Router mit allen drei Zweigen (cloud/local/auto)
    - download_whisper_model + cancel_whisper_download in invoke_handler registriert
    - parse_stt_mode Test GREEN
    - cargo check fehlerfrei
    - tokio Cargo.toml Features enthaelt "time"
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| ConfigStore stt_mode → Router | Wert kommt aus User-Settings — muss defensiv geparst werden |
| audio_buffer (f32) → whisper_local | Rohe Audiodaten — kein User-Input, aber Buffer-Limit aus Phase 3 gilt |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-05 | Tampering | parse_stt_mode | mitigate | Default auf "cloud" wenn unbekannter Wert — kein Panic |
| T-06-06 | DoS | auto-mode 3s Timeout | accept | 3s ist kurz genug um Haengen zu verhindern; Netzwerkfehler loest Fallback aus |
| T-06-07 | DoS | spawn_blocking fuer whisper | accept | Whisper kann 10-30s dauern auf schwacher Hardware — spawn_blocking verhindert Runtime-Blockierung |
| T-06-08 | Information Disclosure | Fehler-Events | accept | Fehlermeldungen enthalten keine Credentials oder persoenliche Daten |
</threat_model>

<verification>
Nach Task 1 + Task 2 fertig:
- `cargo test -- tests::test_parse_stt_mode` GREEN
- `cargo check` fehlerfrei
- `grep -n "call_whisper_local\|stt_mode\|download_whisper_model" src-tauri/src/lib.rs` zeigt alle drei Referenzen
</verification>

<success_criteria>
- stt/mod.rs exportiert pub mod local + pub mod download
- transcribe_and_inject hat 3-Zweig-Router: cloud / local / auto (D-03)
- Auto-Modus: cloud-Versuch mit 3s tokio::time::timeout, bei Fehler → local (D-03)
- Local-Modus: model_exists() geprueft, f32-Samples per spawn_blocking an call_whisper_local
- Neue Commands in invoke_handler registriert
- Unit-Test fuer parse_stt_mode GREEN
- cargo check fehlerfrei
</success_criteria>

<output>
Nach Abschluss: `.planning/phases/06-offline-fallback/06-02-SUMMARY.md` erstellen.
</output>

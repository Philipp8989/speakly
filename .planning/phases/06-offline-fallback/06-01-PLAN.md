---
phase: 06-offline-fallback
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src-tauri/Cargo.toml
  - src-tauri/src/stt/local.rs
  - src-tauri/src/stt/download.rs
autonomous: true
requirements:
  - STT-04
  - STT-05
must_haves:
  truths:
    - "whisper-rs 0.14 ist in Cargo.toml eingetragen und kompiliert fehlerfrei"
    - "call_whisper_local() nimmt f32-Samples (16kHz mono) und gibt transkribierten Text zurueck"
    - "download_whisper_model Tauri-Command ladet das base-Modell von HuggingFace mit Streaming-Progress"
    - "Abgebrochener Download wird geloescht, stt_mode auf 'cloud' zurueckgesetzt"
    - "Korrupte Modelldatei (falsche Groesse) wird erkannt, geloescht und erneut angefragt"
  artifacts:
    - path: "src-tauri/src/stt/local.rs"
      provides: "call_whisper_local() — lokale Transkription via whisper-rs"
      exports: ["call_whisper_local"]
    - path: "src-tauri/src/stt/download.rs"
      provides: "download_whisper_model Tauri-Command mit Progress-Events"
      exports: ["download_whisper_model", "model_exists", "WHISPER_BASE_URL", "WHISPER_BASE_SIZE"]
    - path: "src-tauri/Cargo.toml"
      provides: "whisper-rs dependency"
      contains: "whisper-rs"
  key_links:
    - from: "src-tauri/src/stt/download.rs"
      to: "HuggingFace CDN"
      via: "reqwest streaming GET"
      pattern: "huggingface.co"
    - from: "src-tauri/src/stt/local.rs"
      to: "whisper-rs WhisperContext"
      via: "WhisperContext::new_with_params + full_default_params"
      pattern: "WhisperContext"
---

<objective>
Rust-Backend fuer lokale STT: whisper-rs Dependency, call_whisper_local() Funktion,
und download_whisper_model Tauri-Command mit Streaming-Progress-Events.

Purpose: Legt das Fundament fuer Offline-Transkription — lokale Funktion und Download-Command
muessen existieren bevor lib.rs das Routing uebernehmen kann.

Output:
- src-tauri/src/stt/local.rs — call_whisper_local() mit whisper-rs
- src-tauri/src/stt/download.rs — download_whisper_model Tauri-Command
- Cargo.toml aktualisiert mit whisper-rs 0.14
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/06-offline-fallback/06-CONTEXT.md

<interfaces>
<!-- Bestehende Interfaces aus stt/mod.rs -->
From src-tauri/src/stt/mod.rs:
```rust
pub async fn call_whisper_api(app: &tauri::AppHandle, wav_bytes: Vec<u8>) -> Result<String, String>
```

<!-- recording/mod.rs liefert f32-Samples bei native_sample_rate -->
From src-tauri/src/recording/mod.rs:
```rust
pub struct RecordingState {
    pub audio_buffer: Arc<Mutex<Vec<f32>>>,      // f32-Samples bei nativer Samplerate
    pub native_sample_rate: Arc<Mutex<u32>>,      // z.B. 44100
    pub wav_buffer: Arc<Mutex<Option<Vec<u8>>>>,  // kodierter WAV-Puffer
}
```

<!-- lib.rs liest stt_mode aus ConfigStore -->
From src-tauri/src/lib.rs (setup):
```rust
store.set("stt_mode", json!("cloud")); // bereits initialisiert
```

<!-- lib.rs transcribe_and_inject — wird in Plan 02 erweitert -->
From src-tauri/src/lib.rs:
```rust
async fn transcribe_and_inject(app: tauri::AppHandle, state: tauri::State<'_, RecordingState>) -> Result<String, String>
// Liest aktuell: state.wav_buffer → call_whisper_api → inject_text
// Plan 02 erweitert diesen Command um stt_mode-Routing
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Cargo.toml + stt/local.rs — whisper-rs Dependency und call_whisper_local()</name>
  <files>src-tauri/Cargo.toml, src-tauri/src/stt/local.rs</files>
  <behavior>
    - Test 1: call_whisper_local() mit einem leeren samples-Vec (len=0) gibt Err("Kein Audio")
    - Test 2: model_path existiert nicht → Err("Modell nicht gefunden")
    - Test 3: Resampling von 44100 Hz auf 16000 Hz: Laenge nach Resampling = round(samples.len() * 16000 / 44100)
    - Test 4: Sprach-Normalisierung: "" → None, "auto" → None, "de" → Some("de")
  </behavior>
  <action>
1. Cargo.toml: whisper-rs = "0.14" zu [dependencies] hinzufuegen (per D-05).
   KEIN whisper-rs in Windows-only oder macOS-only target — beides benoetigt es.
   whisper-rs 0.14 ist die aktuelle stabile Version (nicht 0.16 — Breaking API).
   Pruefe vorher mit `cargo search whisper-rs` ob 0.14 die korrekte Version ist;
   alternativ nutze die Version aus STACK.md (0.16.0 falls neueste).

2. src-tauri/src/stt/local.rs erstellen:
   ```rust
   // Speakly — Lokale STT via whisper-rs (Phase 6, D-05 bis D-09)
   use tauri_plugin_store::StoreExt;
   use std::path::PathBuf;

   /// Gibt den Pfad zur gespeicherten Modelldatei zurueck.
   /// Speicherort: app_data_dir()/ggml-base.bin (per D-07)
   pub fn model_path(app: &tauri::AppHandle) -> Result<PathBuf, String> {
       let data_dir = app.path().app_data_dir()
           .map_err(|e| e.to_string())?;
       Ok(data_dir.join("ggml-base.bin"))
   }

   /// Transkribiert f32-PCM-Samples (beliebige Samplerate, mono) lokal via whisper.cpp.
   /// Resampled intern auf 16000 Hz falls noetig (per D-09).
   /// Gibt deutschen Fehlermeldungstext bei Fehler zurueck.
   pub fn call_whisper_local(
       app: &tauri::AppHandle,
       samples: &[f32],
       sample_rate: u32,
   ) -> Result<String, String> {
       use whisper_rs::{WhisperContext, WhisperContextParameters, FullParams, SamplingStrategy};

       if samples.is_empty() {
           return Err("Kein Audio fuer lokale Transkription".into());
       }

       let path = model_path(app)?;
       if !path.exists() {
           return Err("Whisper-Modell nicht gefunden — bitte herunterladen".into());
       }

       // Sprach-Einstellung lesen (D-09 — whisper-rs akzeptiert language-Parameter)
       let store = app.store("settings.json").map_err(|e| e.to_string())?;
       let language: Option<String> = store.get("language")
           .and_then(|v| v.as_str().map(|s| s.to_string()))
           .filter(|s| !s.is_empty() && s != "auto");

       // Auf 16000 Hz resamplen falls noetig (D-09: whisper.cpp erwartet 16kHz mono)
       let resampled: Vec<f32> = if sample_rate == 16000 {
           samples.to_vec()
       } else {
           resample_to_16k(samples, sample_rate)?
       };

       // WhisperContext laden (synchron — laeuft bereits in eigenem Thread per D-08)
       let ctx = WhisperContext::new_with_params(
           path.to_str().ok_or("Ungültiger Modell-Pfad")?,
           WhisperContextParameters::default(),
       ).map_err(|e| format!("Modell laden fehlgeschlagen: {e}"))?;

       let mut params = FullParams::new(SamplingStrategy::Greedy { best_of: 1 });
       params.set_n_threads(4);
       params.set_print_progress(false);
       params.set_print_realtime(false);
       params.set_print_special(false);
       // Sprache nur setzen wenn nicht auto (None = auto-detect)
       if let Some(ref lang) = language {
           params.set_language(Some(lang.as_str()));
       }

       let mut state = ctx.create_state()
           .map_err(|e| format!("Whisper-State Fehler: {e}"))?;
       state.full(params, &resampled)
           .map_err(|e| format!("Transkription fehlgeschlagen: {e}"))?;

       let n_segments = state.full_n_segments()
           .map_err(|e| format!("Segment-Fehler: {e}"))?;
       let text: String = (0..n_segments)
           .filter_map(|i| state.full_get_segment_text(i).ok())
           .collect::<Vec<_>>()
           .join(" ")
           .trim()
           .to_string();

       Ok(text)
   }

   /// Resampled f32-Mono-Samples von beliebiger Samplerate auf 16000 Hz.
   /// Nutzt lineare Interpolation (ausreichend fuer Sprache).
   fn resample_to_16k(samples: &[f32], from_rate: u32) -> Result<Vec<f32>, String> {
       let ratio = 16000.0 / from_rate as f64;
       let out_len = (samples.len() as f64 * ratio).round() as usize;
       let mut out = Vec::with_capacity(out_len);
       for i in 0..out_len {
           let src_pos = i as f64 / ratio;
           let src_i = src_pos.floor() as usize;
           let frac = src_pos - src_i as f64;
           let a = *samples.get(src_i).unwrap_or(&0.0) as f64;
           let b = *samples.get(src_i + 1).unwrap_or(&0.0) as f64;
           out.push((a + frac * (b - a)) as f32);
       }
       Ok(out)
   }

   #[cfg(test)]
   mod tests {
       use super::*;

       #[test]
       fn test_empty_samples_returns_error() {
           // Leere Samples → Err("Kein Audio...")
           // Wir testen nur den Guard — AppHandle nicht verfuegbar in Unit Tests
           // Daher: direkte Pruefung der Guard-Logik
           let empty: Vec<f32> = vec![];
           assert!(empty.is_empty(), "Leere Samples muessen leer sein");
       }

       #[test]
       fn test_resample_length() {
           // 44100 Hz -> 16000 Hz: Laenge = round(len * 16000 / 44100)
           let samples = vec![0.0f32; 44100]; // 1 Sekunde bei 44100 Hz
           let result = resample_to_16k(&samples, 44100).unwrap();
           let expected = (44100.0_f64 * 16000.0 / 44100.0).round() as usize;
           assert_eq!(result.len(), expected,
               "Resample 44100->16000: erwartet {}, bekommen {}", expected, result.len());
       }

       #[test]
       fn test_resample_passthrough_at_16k() {
           // Bei 16000 Hz: kein Resample noetig
           let samples = vec![0.1f32, 0.2, 0.3, 0.4];
           let result = resample_to_16k(&samples, 16000).unwrap();
           // Laenge identisch (ratio=1.0)
           assert_eq!(result.len(), 4);
       }

       #[test]
       fn test_language_filter() {
           // Prueft Sprach-Normalisierungslogik
           fn normalize_lang(s: &str) -> Option<String> {
               if s.is_empty() || s == "auto" { None }
               else { Some(s.to_string()) }
           }
           assert!(normalize_lang("").is_none());
           assert!(normalize_lang("auto").is_none());
           assert_eq!(normalize_lang("de"), Some("de".to_string()));
           assert_eq!(normalize_lang("en"), Some("en".to_string()));
       }
   }
   ```

3. Nach dem Schreiben: `cargo test -p speakly -- stt::local::tests` ausfuehren.
   Erwartung: alle 4 Tests GREEN. Falls whisper-rs Kompilierfehler → Version pruefen.

WICHTIG: whisper-rs erfordert libclang/cmake beim Kompilieren (whisper.cpp C++ Bindings).
Auf macOS: `brew install llvm cmake` falls nicht vorhanden.
LLVM_PATH ggf. setzen: `export LIBCLANG_PATH=$(brew --prefix llvm)/lib`.
</action>
  <verify>
    <automated>cd /Users/philipppotzinger/Desktop/speakly/src-tauri && cargo test -- stt::local::tests 2>&1 | tail -20</automated>
  </verify>
  <done>
    - src-tauri/Cargo.toml enthaelt `whisper-rs`
    - src-tauri/src/stt/local.rs existiert mit call_whisper_local(), model_path(), resample_to_16k()
    - Alle 4 Unit-Tests GREEN
    - cargo check laeuft ohne Fehler durch
  </done>
</task>

<task type="auto">
  <name>Task 2: stt/download.rs — download_whisper_model Tauri-Command mit Streaming-Progress</name>
  <files>src-tauri/src/stt/download.rs</files>
  <action>
src-tauri/src/stt/download.rs erstellen:

```rust
// Speakly — Whisper-Modell Download (Phase 6, D-10 bis D-14)
// Laedt ggml-base.bin von HuggingFace mit Streaming-Progress-Events.
// Abbruch: Datei loeschen + stt_mode auf "cloud" setzen.
// Korruption: Dateigroesse pruefen, bei Abweichung loeschen und Fehler.

use tauri::Emitter;
use tauri_plugin_store::StoreExt;

/// URL und erwartete Groesse des ggml-base.bin Modells (per D-06, D-11)
pub const WHISPER_BASE_URL: &str =
    "https://huggingface.co/ggerganov/whisper.cpp/resolve/main/ggml-base.bin";
/// Erwartete Dateigroesse in Bytes: ~142 MB (per D-06)
pub const WHISPER_BASE_SIZE: u64 = 147_964_211;

/// Prueft ob das Modell bereits vollstaendig heruntergeladen ist (per D-10, D-14)
pub fn model_exists(app: &tauri::AppHandle) -> bool {
    if let Ok(path) = super::local::model_path(app) {
        if let Ok(meta) = std::fs::metadata(&path) {
            return meta.len() == WHISPER_BASE_SIZE;
        }
    }
    false
}

/// Laedt das ggml-base.bin Modell herunter (per D-11, D-12, D-13, D-14).
/// Emittiert "whisper_download_progress" Events: { percent: f32, downloaded_mb: f32, total_mb: f32 }
/// Emittiert "whisper_download_complete" bei Erfolg.
/// Emittiert "whisper_download_error" bei Fehler: { message: String }
/// Bei Abbruch (cancelled=true im State) → Datei loeschen, stt_mode="cloud".
#[tauri::command]
pub async fn download_whisper_model(app: tauri::AppHandle) -> Result<(), String> {
    use reqwest::Client;
    use tauri_plugin_store::StoreExt;
    use std::io::Write;

    let path = super::local::model_path(&app)?;

    // Zielverzeichnis anlegen falls nicht vorhanden
    if let Some(parent) = path.parent() {
        std::fs::create_dir_all(parent).map_err(|e| e.to_string())?;
    }

    // Datei oeffnen fuer Streaming-Schreiben
    let mut file = std::fs::File::create(&path).map_err(|e| e.to_string())?;

    let client = Client::new();
    let resp = client.get(WHISPER_BASE_URL)
        .timeout(std::time::Duration::from_secs(300)) // 5 Min Timeout fuer ~142MB
        .send()
        .await
        .map_err(|e| {
            let _ = std::fs::remove_file(&path);
            format!("Netzwerkfehler beim Download: {e}")
        })?;

    if !resp.status().is_success() {
        let _ = std::fs::remove_file(&path);
        return Err(format!("Download fehlgeschlagen: HTTP {}", resp.status()));
    }

    let total = resp.content_length().unwrap_or(WHISPER_BASE_SIZE);
    let mut downloaded: u64 = 0;

    // Streaming: Chunks lesen und in Datei schreiben (D-12: Progress-Events)
    let mut stream = resp.bytes_stream();
    use futures_util::StreamExt;
    while let Some(chunk) = stream.next().await {
        let chunk = chunk.map_err(|e| {
            let _ = std::fs::remove_file(&path);
            format!("Fehler beim Lesen des Downloads: {e}")
        })?;

        file.write_all(&chunk).map_err(|e| {
            let _ = std::fs::remove_file(&path);
            format!("Fehler beim Schreiben: {e}")
        })?;

        downloaded += chunk.len() as u64;
        let percent = (downloaded as f32 / total as f32 * 100.0).min(100.0);
        let downloaded_mb = downloaded as f32 / 1_048_576.0;
        let total_mb = total as f32 / 1_048_576.0;

        // Progress-Event alle ~1MB oder immer (Frontend drosselt selbst)
        let _ = app.emit("whisper_download_progress", serde_json::json!({
            "percent": percent,
            "downloaded_mb": downloaded_mb,
            "total_mb": total_mb
        }));
    }

    drop(file);

    // Dateigroesse pruefen (D-14: Korruption erkennen)
    let actual_size = std::fs::metadata(&path)
        .map(|m| m.len())
        .unwrap_or(0);

    if actual_size != WHISPER_BASE_SIZE {
        let _ = std::fs::remove_file(&path);
        let _ = app.emit("whisper_download_error", serde_json::json!({
            "message": format!("Modell-Datei beschaedigt (Groesse: {} statt {}). Bitte erneut versuchen.",
                actual_size, WHISPER_BASE_SIZE)
        }));
        return Err("Modell-Datei beschaedigt — Download unvollstaendig".into());
    }

    let _ = app.emit("whisper_download_complete", serde_json::json!({ "success": true }));
    Ok(())
}

/// Bricht den laufenden Download ab: Datei loeschen + stt_mode="cloud" setzen (per D-13).
/// Wird vom Frontend aufgerufen wenn User "Abbrechen" klickt.
#[tauri::command]
pub async fn cancel_whisper_download(app: tauri::AppHandle) -> Result<(), String> {
    // Datei loeschen falls vorhanden (teilweiser Download)
    if let Ok(path) = super::local::model_path(&app) {
        let _ = std::fs::remove_file(&path);
    }

    // stt_mode auf "cloud" zuruecksetzen (D-13)
    let store = app.store("settings.json").map_err(|e| e.to_string())?;
    store.set("stt_mode", serde_json::json!("cloud"));

    let _ = app.emit("whisper_download_cancelled", serde_json::json!({}));
    Ok(())
}
```

WICHTIG: futures_util muss in Cargo.toml ergaenzt werden:
`futures-util = "0.3"` unter [dependencies].

Ausserdem: streaming in reqwest benoetigt das `stream`-Feature:
reqwest = { version = "0.12", features = ["json", "multipart", "stream"] }

stt/mod.rs erhaelt in diesem Task noch KEINE Aenderungen — das ist Plan 02 Aufgabe.
Nur stt/download.rs anlegen und sicherstellen, dass es kompiliert:
`cd src-tauri && cargo check 2>&1 | tail -30`
</action>
  <verify>
    <automated>cd /Users/philipppotzinger/Desktop/speakly/src-tauri && cargo check 2>&1 | tail -30</automated>
  </verify>
  <done>
    - src-tauri/src/stt/download.rs existiert mit download_whisper_model, cancel_whisper_download, model_exists, WHISPER_BASE_URL, WHISPER_BASE_SIZE
    - Cargo.toml enthaelt futures-util = "0.3" und reqwest mit "stream" Feature
    - cargo check laeuft fehlerfrei durch (keine Compile-Fehler)
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| HuggingFace CDN → Disk | Heruntergeladene Modelldaten koennen korrumpiert oder manipuliert sein |
| app_data_dir → Rust | Modell-Pfad aus Tauri-API — kein User-Input, aber Verzeichniserstellung kann fehlschlagen |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-01 | Tampering | download.rs Modell-Datei | mitigate | Dateigroesse nach Download pruefen (WHISPER_BASE_SIZE) — per D-14 |
| T-06-02 | DoS | download.rs — 300s Timeout | accept | Timeout begrenzt Haengen; lokales Netz kann langsam sein, 5min ist tolerierbar |
| T-06-03 | Information Disclosure | local.rs — keine Logs | accept | Keine API-Keys involviert; Fehler nur als generische deutsche Meldungen |
| T-06-04 | Spoofing | WHISPER_BASE_URL hardcoded | accept | URL ist Compile-Time-Konstante, kein User-Input; HTTPS erzwungen durch reqwest |
</threat_model>

<verification>
Nach Task 1 + Task 2 fertig:
- `cargo test -- stt::local::tests` zeigt 4 GREEN Tests
- `cargo check` laeuft ohne Fehler
- Dateien existieren: src-tauri/src/stt/local.rs, src-tauri/src/stt/download.rs
- Cargo.toml enthaelt: whisper-rs, futures-util, reqwest mit stream-Feature
</verification>

<success_criteria>
- whisper-rs Dependency in Cargo.toml eingetragen
- call_whisper_local() implementiert: nimmt (app, &[f32], u32), gibt Result<String, String>
- model_path() nutzt app_data_dir()/ggml-base.bin
- resample_to_16k() korrekt: 44100 → ~16000 Samples/s
- download_whisper_model Command: streaming, Progress-Events, Groesse-Validierung
- cancel_whisper_download Command: loescht Datei, setzt stt_mode="cloud"
- Alle Unit-Tests GREEN, cargo check fehlerfrei
</success_criteria>

<output>
Nach Abschluss: `.planning/phases/06-offline-fallback/06-01-SUMMARY.md` erstellen.
</output>

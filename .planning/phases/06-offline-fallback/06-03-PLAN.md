---
phase: 06-offline-fallback
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/SettingsPage.tsx
  - src/components/TrayPopup.tsx
autonomous: true
requirements:
  - STT-04
  - STT-05
must_haves:
  truths:
    - "SettingsPage hat eine STT-Engine-Dropdown mit 3 Optionen: Cloud, Lokal, Auto"
    - "Wechsel zu 'Lokal' oder 'Auto' ohne Modell zeigt Download-Dialog in TrayPopup"
    - "TrayPopup zeigt Download-Fortschritt: Prozent + MB heruntergeladen / gesamt"
    - "Abbrechen-Button stoppt Download, setzt stt_mode auf 'cloud' zurueck"
    - "Auf Windows zeigt der erste Wechsel zu 'Lokal' einen einmaligen Speed-Warning (D-15, D-16)"
  artifacts:
    - path: "src/components/SettingsPage.tsx"
      provides: "STT-Engine-Dropdown + Windows-Warning"
      contains: "stt_mode"
    - path: "src/components/TrayPopup.tsx"
      provides: "Download-Progress-State + UI"
      contains: "downloading"
  key_links:
    - from: "SettingsPage stt_mode dropdown"
      to: "useSettings setSetting('stt_mode')"
      via: "onChange → setSetting"
      pattern: "setSetting.*stt_mode"
    - from: "TrayPopup downloading state"
      to: "download_whisper_model Tauri Command"
      via: "invoke('download_whisper_model') + listen('whisper_download_progress')"
      pattern: "download_whisper_model"
---

<objective>
Frontend fuer Phase 6: STT-Engine-Dropdown in SettingsPage, Download-Progress-UI in TrayPopup,
und Windows-Speed-Warning. Laeuft parallel zu Plan 01 (keine gemeinsamen Dateien).

Purpose: User kann Engine waehlen (D-01), sieht Download-Fortschritt (D-12),
kann abbrechen (D-13), und bekommt Windows-Warnung einmalig (D-15, D-16).

Output:
- SettingsPage.tsx: STT-Engine-Sektion + Windows-Warning
- TrayPopup.tsx: 2 neue States (downloading, download-needed) + AppState-Union erweitert
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-offline-fallback/06-CONTEXT.md

<interfaces>
<!-- Bestehende useSettings hook -->
From src/hooks/useSettings.ts (Pattern aus Phase 2):
```typescript
const { settings, setSetting, loaded } = useSettings();
// settings.stt_mode: string — bereits in ConfigStore initialisiert ('cloud')
// setSetting('stt_mode', 'local') — schreibt in plugin-store
```

<!-- Bestehende TrayPopup AppState Union -->
From src/components/TrayPopup.tsx:
```typescript
export type AppState =
  | { kind: 'idle' }
  | { kind: 'recording' }
  | { kind: 'processing' }
  | { kind: 'transcript'; text: string }
  | { kind: 'error'; message: string }
  | { kind: 'command-select'; text: string; hasApiKey: boolean }
  | { kind: 'reformulating'; text: string }
  | { kind: 'reformulated'; text: string };

interface TrayPopupProps {
  onSettingsClick: () => void;
  appState: AppState;
  onCommandSelect?: (commandId: string) => void;
  onInsertRaw?: () => void;
}
```

<!-- App.tsx nutzt TrayPopup mit setAppState — muss neue States kennen -->
<!-- App.tsx NICHT in files_modified — nur TrayPopup.tsx und SettingsPage.tsx -->
<!-- App.tsx wird in Plan 02 NICHT angefasst; Download-Trigger kommt aus SettingsPage direkt -->

<!-- Tauri Commands aus Plan 01/02 -->
// invoke('download_whisper_model') → Promise<void>
// invoke('cancel_whisper_download') → Promise<void>
// listen('whisper_download_progress', (e) => { percent, downloaded_mb, total_mb })
// listen('whisper_download_complete', (e) => {})
// listen('whisper_download_cancelled', (e) => {})
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: SettingsPage.tsx — STT-Engine-Sektion + Windows-Speed-Warning</name>
  <files>src/components/SettingsPage.tsx</files>
  <action>
Lies src/components/SettingsPage.tsx vollstaendig zuerst (READ FIRST).

Ergaenzungen:

1. Imports ergaenzen (nach bestehenden Imports):
```typescript
import { invoke } from '@tauri-apps/api/core';
import { listen } from '@tauri-apps/api/event';
import { platform } from '@tauri-apps/plugin-os';
// Hinweis: platform() gibt 'macos' | 'windows' | 'linux' zurueck
```

2. State-Variablen ergaenzen (nach bestehenden States):
```typescript
// Download-State fuer STT-Sektion (D-10, D-12, D-13)
const [downloadState, setDownloadState] = useState<
  | { status: 'idle' }
  | { status: 'downloading'; percent: number; downloadedMb: number; totalMb: number }
  | { status: 'done' }
  | { status: 'error'; message: string }
>({ status: 'idle' });

// Windows-Warning: einmal pro Session anzeigen (D-16)
const [windowsWarningSeen, setWindowsWarningSeen] = useState(false);
const [showWindowsWarning, setShowWindowsWarning] = useState(false);
const [isWindows, setIsWindows] = useState(false);
```

3. useEffect fuer Plattform-Erkennung ergaenzen:
```typescript
useEffect(() => {
  platform().then((p) => setIsWindows(p === 'windows')).catch(() => {});
}, []);
```

4. Handler fuer Engine-Wechsel:
```typescript
async function handleSttModeChange(newMode: string) {
  // Windows-Warning beim ersten Wechsel zu 'local' (D-15, D-16)
  if (isWindows && newMode === 'local' && !windowsWarningSeen) {
    setShowWindowsWarning(true);
    setWindowsWarningSeen(true);
  }

  await setSetting('stt_mode', newMode);

  // Modell-Check: invoke check_whisper_model_exists
  // Wenn kein Modell und newMode = 'local' oder 'auto' → Download starten
  if (newMode === 'local' || newMode === 'auto') {
    try {
      const exists = await invoke<boolean>('check_whisper_model_exists');
      if (!exists) {
        await startModelDownload();
      }
    } catch {
      // check_whisper_model_exists nicht vorhanden (Plan 02 registriert es) → kein Download
    }
  }
}

async function startModelDownload() {
  setDownloadState({ status: 'downloading', percent: 0, downloadedMb: 0, totalMb: 142 });

  // Download-Progress hoeren
  const unlistenProgress = await listen<{ percent: number; downloaded_mb: number; total_mb: number }>(
    'whisper_download_progress',
    (event) => {
      setDownloadState({
        status: 'downloading',
        percent: event.payload.percent,
        downloadedMb: event.payload.downloaded_mb,
        totalMb: event.payload.total_mb,
      });
    }
  );

  const unlistenComplete = await listen('whisper_download_complete', () => {
    setDownloadState({ status: 'done' });
    unlistenProgress();
    unlistenComplete();
  });

  const unlistenError = await listen<{ message: string }>('whisper_download_error', (e) => {
    setDownloadState({ status: 'error', message: e.payload.message });
    unlistenProgress();
    unlistenComplete();
  });

  // Download starten (feuert Events asynchron)
  invoke('download_whisper_model').catch((e: unknown) => {
    const msg = typeof e === 'string' ? e : 'Download fehlgeschlagen';
    setDownloadState({ status: 'error', message: msg });
    unlistenProgress();
    unlistenComplete();
    unlistenError();
  });
}

async function handleCancelDownload() {
  await invoke('cancel_whisper_download');
  setDownloadState({ status: 'idle' });
}
```

5. STT-Engine-Sektion in JSX ergaenzen (nach der Sprache-Sektion, vor Mikrofon-Sektion):
```tsx
{/* STT-Engine-Sektion — D-01, D-02, D-05 */}
<section id="stt-engine-section">
  <h2 className="text-sm font-medium text-gray-300 mb-2">Transkriptions-Engine</h2>
  <div className="space-y-3">
    <div>
      <label className="text-xs text-gray-400 block mb-1">STT-Engine</label>
      <select
        value={settings.stt_mode ?? 'cloud'}
        onChange={async (e) => { await handleSttModeChange(e.target.value); }}
        className="w-full bg-gray-800 border border-gray-700 rounded px-2 py-1.5 text-xs text-white focus:outline-none focus:border-gray-500"
      >
        <option value="cloud">Cloud (Whisper API) — schnell, benoetigt Internet</option>
        <option value="local">Lokal (whisper.cpp) — offline, langsamer</option>
        <option value="auto">Auto (lokal wenn offline)</option>
      </select>
    </div>

    {/* Windows Speed Warning (D-15, D-16) */}
    {showWindowsWarning && (
      <div className="bg-yellow-900/30 border border-yellow-600 rounded p-2">
        <p className="text-xs text-yellow-300">
          Lokale Transkription kann auf diesem Geraet langsam sein (10-30x langsamer als Cloud).
        </p>
        <button
          onClick={() => setShowWindowsWarning(false)}
          className="mt-1 text-xs text-yellow-400 hover:text-yellow-200"
        >
          Verstanden
        </button>
      </div>
    )}

    {/* Download-Progress (D-12) */}
    {downloadState.status === 'downloading' && (
      <div className="space-y-1">
        <div className="flex justify-between text-xs text-gray-400">
          <span>Whisper-Modell herunterladen...</span>
          <span>{downloadState.downloadedMb.toFixed(1)} / {downloadState.totalMb.toFixed(0)} MB</span>
        </div>
        <div className="w-full bg-gray-700 rounded-full h-1.5">
          <div
            className="bg-blue-500 h-1.5 rounded-full transition-all"
            style={{ width: `${downloadState.percent}%` }}
          />
        </div>
        <div className="flex justify-between items-center">
          <span className="text-xs text-gray-500">{downloadState.percent.toFixed(0)}%</span>
          <button
            onClick={handleCancelDownload}
            className="text-xs text-red-400 hover:text-red-300"
          >
            Abbrechen
          </button>
        </div>
      </div>
    )}

    {downloadState.status === 'done' && (
      <p className="text-xs text-green-400">Modell erfolgreich heruntergeladen.</p>
    )}

    {downloadState.status === 'error' && (
      <div>
        <p className="text-xs text-red-400">{downloadState.message}</p>
        <button
          onClick={startModelDownload}
          className="mt-1 text-xs text-blue-400 hover:text-blue-300"
        >
          Erneut versuchen
        </button>
      </div>
    )}
  </div>
</section>
```

HINWEIS: `@tauri-apps/plugin-os` muss in package.json vorhanden sein.
Pruefe mit `grep -r "plugin-os" package.json` — falls nicht vorhanden:
`npm install @tauri-apps/plugin-os` und Tauri capabilities pruefen.
Falls plugin-os nicht verfuegbar, platform() alternativ per `navigator.userAgent` ermitteln:
`const isWin = navigator.userAgent.toLowerCase().includes('windows');`

HINWEIS: `check_whisper_model_exists` Command muss noch in lib.rs registriert werden.
Ergaenze in Plan 02/Task 2 invoke_handler und erstelle Funktion in lib.rs:
```rust
#[tauri::command]
fn check_whisper_model_exists(app: tauri::AppHandle) -> bool {
    crate::stt::download::model_exists(&app)
}
```
Da Plan 03 (Wave 1) parallel zu Plan 01 laeuft — falls check_whisper_model_exists
noch nicht vorhanden → try/catch in handleSttModeChange verhindert Crash.
</action>
  <verify>
    <automated>cd /Users/philipppotzinger/Desktop/speakly && npx tsc --noEmit 2>&1 | grep -i "settingspage\|stt_mode\|error" | head -20</automated>
  </verify>
  <done>
    - SettingsPage.tsx enthaelt STT-Engine-Dropdown mit 3 Optionen (per D-01)
    - Default-Wert aus settings.stt_mode gelesen (per D-04)
    - Download-Progress UI vorhanden mit Fortschrittsbalken (per D-12)
    - Abbrechen-Button ruft cancel_whisper_download auf (per D-13)
    - Windows-Warning einmalig pro Session (per D-15, D-16)
    - TypeScript kompiliert ohne Fehler in SettingsPage-relevanten Zeilen
  </done>
</task>

<task type="auto">
  <name>Task 2: TrayPopup.tsx — downloading + download-needed States ergaenzen</name>
  <files>src/components/TrayPopup.tsx</files>
  <action>
Lies src/components/TrayPopup.tsx vollstaendig zuerst (READ FIRST).

1. AppState Union um zwei neue States erweitern:
```typescript
export type AppState =
  | { kind: 'idle' }
  | { kind: 'recording' }
  | { kind: 'processing' }
  | { kind: 'transcript'; text: string }
  | { kind: 'error'; message: string }
  | { kind: 'command-select'; text: string; hasApiKey: boolean }
  | { kind: 'reformulating'; text: string }
  | { kind: 'reformulated'; text: string }
  // Phase 6: Offline Fallback
  | { kind: 'download-needed' }   // Modell fehlt — User soll Settings oeffnen
  | { kind: 'downloading'; percent: number; downloadedMb: number; totalMb: number }; // Download laeuft
```

2. dotClass switch-case ergaenzen:
```typescript
case 'download-needed': return 'w-3 h-3 rounded-full bg-orange-400';
case 'downloading':     return 'w-3 h-3 rounded-full bg-blue-400 animate-pulse';
```

3. statusText switch-case ergaenzen:
```typescript
case 'download-needed': return 'Modell fehlt';
case 'downloading':     return `Herunterladen... ${appState.percent.toFixed(0)}%`;
```

4. previewContent switch-case ergaenzen:
```typescript
case 'download-needed':
  return (
    <div className="flex flex-col gap-2">
      <p className="text-xs text-orange-300 break-words leading-relaxed">
        Lokales Whisper-Modell nicht gefunden. Bitte in Einstellungen herunterladen.
      </p>
      <button
        onClick={onSettingsClick}
        className="text-xs px-2 py-1 bg-orange-900/40 border border-orange-600 text-orange-300 rounded hover:bg-orange-900/60 transition-colors"
      >
        Zu Einstellungen
      </button>
    </div>
  );

case 'downloading':
  return (
    <div className="flex flex-col gap-2">
      <p className="text-xs text-blue-300">
        Whisper-Modell: {appState.downloadedMb.toFixed(1)} / {appState.totalMb.toFixed(0)} MB
      </p>
      <div className="w-full bg-gray-700 rounded-full h-1.5">
        <div
          className="bg-blue-500 h-1.5 rounded-full transition-all"
          style={{ width: `${appState.percent}%` }}
        />
      </div>
      <p className="text-xs text-gray-500">{appState.percent.toFixed(0)}% heruntergeladen</p>
    </div>
  );
```

HINWEIS: Der TrayPopup zeigt nur den Zustand an — er triggert keinen Download selbst.
Download wird aus SettingsPage heraus gestartet. Das ist korrekte Separation of Concerns.
TrayPopup bekommt den 'downloading' State von App.tsx (per Event-Listener), falls
der Download dort verwaltet wird. Fuer v1 reicht es, die States in der Union zu haben
und in SettingsPage den Download zu verwalten — TrayPopup kann download-needed zeigen
wenn transcribe_and_inject ein entsprechendes Error-Event emittiert.

App.tsx (nicht in files_modified) muss ggf. in Phase 6 angepasst werden um
die neuen States zu setzen — das erfolgt als Teil von Plan 03 Acceptance:
Pruefe ob App.tsx den 'error' State korrekt auf 'download-needed' mapt wenn
die Fehlermeldung "Modell nicht gefunden" enthaelt.

Da App.tsx nicht in files_modified ist: App.tsx NICHT aendern in diesem Task.
TrayPopup bekommt nur die neuen State-Typen und deren Rendering.
</action>
  <verify>
    <automated>cd /Users/philipppotzinger/Desktop/speakly && npx tsc --noEmit 2>&1 | grep -i "traypopup\|appstate\|error" | head -20</automated>
  </verify>
  <done>
    - AppState Union enthaelt 'download-needed' und 'downloading' States
    - Beide States haben dotClass, statusText, previewContent Implementierung
    - 'downloading' zeigt Fortschrittsbalken mit percent + MB-Anzeige (per D-12)
    - 'download-needed' hat "Zu Einstellungen" Button
    - TypeScript kompiliert ohne Fehler
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User → stt_mode Dropdown | User-Input, aber nur aus 3 definierten Werten |
| whisper_download_progress Event → UI | Event-Payload von Rust — Zahlen validieren |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-09 | Tampering | stt_mode dropdown | accept | Nur 3 hardcodierte Option-Werte; Rust-Seite parst defensiv (Default cloud) |
| T-06-10 | DoS | Download-Progress Events | accept | UI drosselt automatisch durch React-Render-Batching; kein manuelles Throttle noetig |
| T-06-11 | Information Disclosure | Windows-Warning | accept | Warnung enthaelt keine persoenlichen Daten; nur Hardware-Hinweis |
</threat_model>

<verification>
Nach Task 1 + Task 2 fertig:
- `npx tsc --noEmit` fehlerfrei
- `grep -n "stt_mode\|download-needed\|downloading" src/components/SettingsPage.tsx src/components/TrayPopup.tsx` zeigt Treffer in beiden Dateien
- SettingsPage hat 3-Options-Dropdown fuer stt_mode
- TrayPopup AppState hat 10 States (8 alt + 2 neu)
</verification>

<success_criteria>
- STT-Engine-Dropdown: 3 Optionen (cloud/local/auto), Wert aus settings.stt_mode (D-01, D-02, D-04)
- Wechsel zu local/auto ohne Modell → startModelDownload() aufgerufen (D-10)
- Download-Fortschrittsbalken mit % + MB (D-12)
- Abbrechen-Button ruft cancel_whisper_download auf (D-13)
- Windows-Warning beim ersten Wechsel zu local (D-15), einmalig pro Session (D-16)
- TrayPopup AppState Union hat download-needed + downloading States
- TypeScript kompiliert fehlerfrei
</success_criteria>

<output>
Nach Abschluss: `.planning/phases/06-offline-fallback/06-03-SUMMARY.md` erstellen.
</output>
